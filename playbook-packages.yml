---
- name: Installation du paquet apache2 sur Debian
  hosts: deby-cli
  gather_facts: false
  become: true

  tasks:
    - name: Installation d'Apache
      ansible.builtin.package:
        name: apache2
        update_cache: true
